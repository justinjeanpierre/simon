# simon
Simulator controller.


Install requirements: `pip install -r requirements.txt`

Start the app: `python simon.py`

Run /jobs tests with `python -m test.jobs_route_tests`

Run /users tests with `python -m test.users_route_tests`
